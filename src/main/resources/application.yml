server:
  port: 8080
  error:
    whitelabel:
      enabled: false
    path: /error
    include-stacktrace: never

spring:
  main:
    web-application-type: servlet
  datasource:
    url: jdbc:postgresql://localhost:5432/gestion_des_consultations
    username: MohamedFrh
    password: khouyafiras28
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
